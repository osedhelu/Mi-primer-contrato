{
  "name": "yafux",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "npx truffle compile",
    "local-chain": "export NODE_OPTIONS=--openssl-legacy-provider && npx ganache-cli --deterministic",
    "migrate": "npx truffle migrate --network development",
    "console": "npx truffle console --network development"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@truffle/hdwallet-provider": "^1.7.0",
    "ganache-cli": "^6.12.2",
    "truffle": "^5.4.21"
  }
}
